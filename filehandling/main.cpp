#include<iostream>
#include<string>
using namespace std;


int main(){
    static const char * originalfile = "original.txt";
    static const char * editedfile = "edited.txt";

    //rename(originalfile , editedfile);                // for renaming the file
    //remove(editedfile);                               // for removing the file
    //FILE * fh = fopen(originalfile, "w");             // for opening and creating the file
    //fclose(fh);                                       // for closing the file

    return 0;
}